# Local-only files — never push
CLAUDE.md

# IntelliJ / JetBrains project files
.idea/

# EUDI issuer keys — generated at first startup, never committed
actid/backend/app/keys/*.pem
actid/backend/app/keys/*.jwk
